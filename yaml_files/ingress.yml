kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: deletetasktodoingress
spec:
  rules:
    - host: deletetasktodo.mano.com
      http:
        paths:
          - backend:
              service:
                name: deletetasktodoservice
                port:
                  number: 80
            path: "/"
            pathType: Prefix
  ingressClassName: nginx

